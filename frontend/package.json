{
  "name": "nrw-charging-atlas-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "build:public": "tsc && vite build --mode public",
    "test": "vitest run",
    "test:public": "playwright test"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/leaflet": "^1.9.12",
    "typescript": "^5.5.4",
    "vite": "7.3.6",
    "vitest": "4.1.11",
    "wrangler": "4.131.2"
  }
}
